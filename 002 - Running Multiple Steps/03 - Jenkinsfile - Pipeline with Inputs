pipeline {
    agent { 
		docker { 
			image 'alpine:latest' 
		}
	}
    stages {
        stage('build') {
	input{
		parameters{
		string (name: 'NAME')
		string (age: 'AGE')
		}
	}
            steps {
                echo "$NAME"
                echo "$AGE"
            }
        }
    }
}
